# CyberSurvivor Backend Environment Variables
# Copy to .env (do NOT commit real secrets)

# Server
PORT=3000

# Google Auth (optional for scorelog)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# MySQL (optional if using only Redis)
DB_HOST=a066um.forpsi.com
DB_USER=f190888
DB_PASS=REPLACE_ME
DB_NAME=f190888
DB_PORT=3306

# Enable dual write (Redis + MySQL)
DUAL_WRITE_MYSQL=0

# Redis (Upstash) - required for new leaderboard
UPSTASH_REDIS_REST_URL=https://your-upstash-url
UPSTASH_REDIS_REST_TOKEN=your-upstash-token

# Protect migration endpoint
MIGRATION_SECRET=choose-a-random-secret

# Optional: restrict allowed game modes (comma separated, empty = allow all)
LB_ALLOWED_MODES=SHOWDOWN

# Optional: restrict allowed character IDs (comma separated, * or empty = all)
LB_ALLOWED_CHARS=runner,tech_warrior

# Optional: HMAC secret to sign submissions (enable anti-tamper)
LB_HMAC_SECRET=replace_with_random_hex
LB_HMAC_SOFT=1

# Anti-cheat soft mode (1 logs & allows, 0 blocks flagged submissions)
LB_ANTICHEAT_SOFT=1

# Allowed modes/chars (see above) already defined; pagination supported via ?start param on /api/leaderboard/top
