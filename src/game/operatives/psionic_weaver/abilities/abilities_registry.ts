import type { AbilityDescriptor, PassiveHooks } from '../../ability-types';
import { LatticeSpace } from './lattice_space';

export const abilities: AbilityDescriptor[] = [LatticeSpace];
export const passiveHooks: PassiveHooks = {};
