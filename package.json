{
  "name": "cyber-survivor-ts",
  "version": "0.3.1",
  "private": true,
  "description": "CyberSurvivor game (Web / Vite + TypeScript)",
  "license": "UNLICENSED",
  "scripts": {
  "dev": "vite",
    "build:renderer": "vite build",
    "build": "vite build",
    "preview": "vite preview",
  "backend": "echo backend removed",
  "clean": "rimraf dist",
    "version:patch": "npm version patch -m 'chore(release): bump version to %s'",
  "postinstall": "echo (no electron postinstall)",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "lint:security": "echo (placeholder for future security lint)"
  },
  "build": {},
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/howler": "^2.2.12",
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.0.0",
    "vite": "^7.1.2",
    "vitest": "^3.2.4",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
  "howler": "^2.2.4"
  }
}
