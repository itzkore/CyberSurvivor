###############################################
# CyberSurvivor Environment Example (.env.example)
# Copy to .env.local (gitignored) and fill values
###############################################

# === Leaderboard (Upstash Redis REST) ===
# Full base REST URL from Upstash dashboard (ends with .upstash.io)
UPSTASH_REDIS_REST_URL="https://sacred-killdeer-11197.upstash.io"
# Access token limited to: ZADD ZSCORE ZREVRANGE HSET HGET EXPIRE
UPSTASH_REDIS_REST_TOKEN="ASu9AAIncDEzYzBmZGQxODhlM2I0YTg2OGQ1Yzg5MTA5YTIxOTUyY3AxMTExOTc"

# === Google OAuth (renderer, web implicit/one-tap) ===
# Web client ID (kept in meta fallback too). Safe-ish to expose.
VITE_GOOGLE_CLIENT_ID=156752381672-vidmkis66cs201c39ps9vac3230bv6rl.apps.googleusercontent.com

# === Electron Native PKCE (desktop OAuth) ===
# Desktop client ID (NOT the web one). Only used inside Electron main process.
GOOGLE_OAUTH_CLIENT_ID=

# === Optional Performance / Diagnostics ===
# Unlock FPS / disable vsync (heavier power usage)
# UNLOCK_FPS=1
# GPU flag profile: aggressive | baseline | minimal
# GFX=baseline

# === Build Tweaks ===
# Set to production when packaging manually if needed
# NODE_ENV=production
